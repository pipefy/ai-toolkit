# CLI commands and flags

- **MCP-only.** The 4 meta-tools have no CLI twin (deferred in [docs/parity.md](https://github.com/pipefy/ai-toolkit/blob/main/docs/parity.md)); `add_service_account_to_pipe` is available on both MCP and CLI.
