# CLI reference

The CLI routes operations and introspection automatically; both endpoints derive from `PIPEFY_BASE_URL`. For Tier 1 operations and Tier 2 introspection / GraphQL execution, use the CLI reference in `pipefy-introspection` and the relevant domain skill before falling back to direct HTTP.
