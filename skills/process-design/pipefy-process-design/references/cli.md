# CLI reference

For `search_pipes`, use `pipefy pipe list` as documented in `pipefy-pipes-and-cards`. For `get_organization`, use `pipefy org get`. Consult the domain skill selected by `pipefy-building` when the design is ready for execution.
